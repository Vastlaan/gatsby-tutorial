import React from "react"
import styles from "../css_modules/footer.module.scss"

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <h1 className={styles.text}>This is Footer</h1>
    </footer>
  )
}

export default Footer
