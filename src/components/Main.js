import React from "react"
import styles from "../css_modules/home.module.scss"

const Main = () => {
  return (
    <main className={styles.page}>
      <h1 className={styles.header}>This is Home Page</h1>
    </main>
  )
}

export default Main
